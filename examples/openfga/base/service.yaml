apiVersion: v1
kind: Service
metadata:
  name: openfga
  namespace: openfga
spec:
  selector:
    app: openfga
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 3000
      targetPort: 3000
