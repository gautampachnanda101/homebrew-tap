apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

resources:
  - namespace.yaml
  - postgres.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: keycloak
