apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: vault
spec:
  secretName: vault-tls
  issuerRef:
    name: ISSUER_PLACEHOLDER
    kind: ClusterIssuer
  dnsNames:
    - DOMAIN.PLACEHOLDER
