apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: harbor

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - core.yaml
  - registry.yaml
  - portal.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: harbor
