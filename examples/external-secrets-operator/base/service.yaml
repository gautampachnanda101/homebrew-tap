apiVersion: v1
kind: Service
metadata:
  name: external-secrets
  namespace: external-secrets
spec:
  selector:
    app: external-secrets
  ports:
    - name: http
      port: 80
      targetPort: 8080
