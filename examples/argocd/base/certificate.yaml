apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-server-cert
  namespace: argocd
spec:
  secretName: argocd-server-tls
  issuerRef:
    name: ISSUER_NAME  # Will be patched by overlay
    kind: ClusterIssuer
  dnsNames:
    - DOMAIN_NAME  # Will be patched by overlay
